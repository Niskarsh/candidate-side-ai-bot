{
  "name": "candidate-orchestrator",
  "private": true,
  "workspaces": ["apps/server", "apps/web"],
  "scripts": {
    "dev": "concurrently -n SERVER,WEB -c blue,green \"npm:dev --workspace=apps/server\" \"npm:start --workspace=apps/web\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
